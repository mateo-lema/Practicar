    package controllers;

    import models.Car;

    public class CarController {

        // public void SortByAgeBubbleSort(Car[] carros) {
        //     for (int i = 0; i < carros.length; i++) {
        //         boolean swap = false;
        //         for (int j = 0; j < carros.length - 1 - i; j++) {
        //             if (carros[j].getYear() > carros[j + 1].getYear()) {
        //                 Car aux = carros[j];
        //                 carros[j] = carros[j + 1];
        //                 carros[j + 1] = aux;
        //                 swap = true;
        //             }
        //         }
        //         if (swap == false) {
        //             break;
        //         }
        //     }

        // }

        // public void sortByNameSelection(Car[] carros) {
        //     for (int i = 0; i < carros.length; i++) {
        //         int indiceMenor = i;
        //         for (int j = i+1; j < carros.length; j++) {
        //             if (carros[j].getName().compareTo(carros[indiceMenor].getName()) < 0) {
        //                 indiceMenor = j;
        //             }
        //         }
        //         if (i != indiceMenor) {
        //             Car aux = carros[i];
        //             carros[i] = carros[indiceMenor];
        //             carros[indiceMenor] = aux;
        //         }
        //     }

        // }

        // public void sortByNameInsertion(Car[] carros){
        //         for(int i=1;i<carros.length;i++){
        //             Car aux=carros[i];
        //             int j=i-1;
        //             while(j>=0 && carros[j].getName().compareTo(aux.getName())<0){
        //                 carros[j+1]=carros[j];
        //                 j--;
        //             }
        //             carros[j+1]=aux;
        //         }

        // }

    }
